// Generated by CoffeeScript 1.10.0
(function() {
  window.onload = function() {
    var draw, formatDate, height, line, margin, params, parseDate, svg, width, x, xAxis, xhttp, y, yAxis;
    xhttp = new XMLHttpRequest();
    params = "lorem=ipsum&name=binny";
    xhttp.onreadystatechange = function() {
      if (xhttp.readyState !== 4 || xhttp.status !== 200) {
        return;
      }
      return draw(JSON.parse(xhttp.responseText));
    };
    xhttp.open('POST', 'data', true);
    xhttp.setRequestHeader("Content-type", "application/x-www-form-urlencoded");
    xhttp.send(params);
    margin = {
      top: 20,
      right: 20,
      bottom: 70,
      left: 40
    };
    width = 960 - margin.left - margin.right;
    height = 500 - margin.top - margin.bottom;
    formatDate = d3.time.format('%d-%b-%y');
    parseDate = d3.time.format('%Y-%m-%dT%H:%M:%S').parse;
    x = d3.time.scale().range([0, width]);
    y = d3.scale.linear().range([height, 0]);
    xAxis = d3.svg.axis().scale(x).orient('bottom');
    yAxis = d3.svg.axis().scale(y).orient('left');
    line = d3.svg.line().x(function(d) {
      return x(d.date);
    }).y(function(d) {
      return y(d.close);
    });
    svg = d3.select('body').append('svg').attr('width', width + margin.left + margin.right).attr('height', height + margin.top + margin.bottom).append('g').attr('transform', 'translate(' + margin.left + ',' + margin.top + ')');
    return draw = function(data) {
      data.forEach(function(d) {
        d.date = parseDate(d.time.split('.')[0]);
        return d.close = d.temp;
      });
      x.domain(d3.extent(data, function(d) {
        return d.date;
      }));
      y.domain(d3.extent(data, function(d) {
        return d.close;
      }));
      svg.append('g').attr('class', 'x axis').attr('transform', 'translate(0,' + height + ')').call(xAxis);
      svg.append('g').attr('class', 'y axis').call(yAxis).append('text').attr('transform', 'rotate(-90)').attr('y', 6).attr('dy', '.71em').style('text-anchor', 'end').text('Price ($)');
      return svg.append('path').datum(data).attr('class', 'line').attr('d', line);
    };
  };

}).call(this);
